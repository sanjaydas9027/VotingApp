version: '3'
services:
  vote:
    image: <vote-image-name>
    depends_on:
      - redis
    ports:
      - "5000:80"
    networks:
      - front-tier
      - back-tier

  result:
    image: <result-image-name>
    depends_on:
      - db
    ports:
      - "5001:80"
    networks:
      - front-tier
      - back-tier

  worker:
    image: <worker-image-name>
    depends_on:
      - redis
      - db
    networks:
      - back-tier

  redis:
    image: redis:alpine
    networks:
      - back-tier

  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
    volumes:
      - "db-data:/var/lib/postgresql/data"
    networks:
      - back-tier

volumes:
  db-data:

networks:
  front-tier:
  back-tier:
